[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# startCommand omitted — uses Dockerfile ENTRYPOINT which starts Xvfb + uvicorn
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
