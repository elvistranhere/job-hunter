# Database (Neon Postgres)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Gemini API key (for resume parsing)
GEMINI_API_KEY="your-gemini-api-key"

# Anthropic API key (optional — not required for core functionality)
# ANTHROPIC_API_KEY="sk-ant-..."

# Python Worker (Railway or local)
WORKER_URL="https://your-worker.railway.app"
WORKER_SECRET="your-shared-secret"

# Email (Gmail SMTP) — used by Python worker
GMAIL_USER="your@gmail.com"
GMAIL_APP_PASSWORD="your-app-password"
